#include <stdio.h>

 int main(){

    int numero = 4, resultado;

    resultado = numero % 2;

        printf("a variavel resultado é: %d\n", resultado);

       if(resultado == 0) {
            printf("o numero é par\n");
       }
}